//
// Trivial test PTX kernel: each thread does nothing (no global ptr).
// Used to validate the CUDA backend can register + dispatch a real
// PTX module on the host's GPU.
//
.version 7.0
.target sm_70
.address_size 64

.visible .entry mul3(
    .param .u32 dummy
)
{
    ret;
}
